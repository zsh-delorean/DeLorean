@delorean.exec.util.is-not-sudo.stderr.is-sudo () {
  @delorean.import '~/util/stderr-prefix/'
  @delorean.util.stderr-prefix

<<EOF >&2

${DELOREAN_TRUNK[T]}An encounter with sudo could create a time paradox.
${DELOREAN_TRUNK[I]}
${DELOREAN_TRUNK[I]}The results of which could cause a chain reaction that would unravel the
${DELOREAN_TRUNK[I]}very fabric of the space-time continuum and destroy the entire universe!
${DELOREAN_TRUNK[I]}
${DELOREAN_TRUNK[I]}... Granted, that's the worst-case scenario. The destruction however
${DELOREAN_TRUNK[L]}might be limited merely to our own galaxy.

EOF
}
